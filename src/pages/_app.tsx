import "@chatscope/chat-ui-kit-styles/dist/default/styles.min.css"; // Import the global CSS

function MyApp({ Component, pageProps }) {
  return <Component {...pageProps} />;
}

export default MyApp;
